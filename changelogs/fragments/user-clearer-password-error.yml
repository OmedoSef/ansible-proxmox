---
bugfixes:
  - user - surface a clear, actionable error when setting a password for a
    non-C(pve)-realm user (for example C(pam)) fails because the underlying
    system/LDAP/AD account does not exist yet, instead of only the raw
    Proxmox API error.
minor_changes:
  - user - clarify in the documentation that C(password) is forwarded to the
    realm's own password mechanism for non-C(pve) realms rather than being
    ignored.
